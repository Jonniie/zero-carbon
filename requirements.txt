# JéGO Zero Carbon — Intern Assignment
# Python 3.10+ required
# Setup: python -m venv jego-intern-env
#source jego-intern-env/bin/activate
#pip install -r requirements.txt
# Core data processing
pandas>=2.0.0
numpy>=1.24.0
# Geospatial & mapping
folium>=0.15.0
osmnx>=1.6.0
shapely>=2.0.0
overpy>=0.7.0
# Visualisation
matplotlib>=3.7.0
# Scientific (optional — for time-series integration)
scipy>=1.11.0
# Notebook environment
jupyterlab>=4.0.0
# Stretch task only (Part 4 — anomaly detection)
scikit-learn>=1.3.0
pyod>=1.1.0
